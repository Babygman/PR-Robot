# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5

# --- Settings / env ---
# หมายเหตุ: อัปเดตจาก 2.10.4 -> 2.13.5 เพราะ google-genai (Phase 4) ต้องการ pydantic>=2.12.5
pydantic==2.13.5
email-validator==2.2.0
pydantic-settings==2.15.0
python-dotenv==1.0.1

# --- Database / ORM / Migration ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Auth ---
# หมายเหตุ: ใช้ bcrypt ตรงๆ ไม่ผ่าน passlib (เข้ากันไม่ได้กับ bcrypt รุ่นใหม่ — ดูคอมเมนต์ใน app/core/security.py)
bcrypt==5.0.0
python-jose[cryptography]==3.3.0

# --- AI extraction (Google Gemini, Free Tier) ---
# หมายเหตุ: google-generativeai รุ่นเก่าเลิกใช้แล้ว (Deprecated) เปลี่ยนมาใช้ google-genai
# ตรวจสอบยืนยันจาก Google AI Docs / GitHub googleapis/python-genai จริงวันที่ 2026-09-02
google-genai==2.21.0

# --- PDF generation (Phase 5) ---
weasyprint==63.1
